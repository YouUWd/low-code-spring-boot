# JDEC 数据库设计标准


---

## 📋 目录

1. [核心原则](#核心原则)
2. [命名规范](#命名规范)
3. [数据类型](#数据类型)
4. [审计字段](#审计字段)
5. [索引规范](#索引规范)
6. [约束规范](#约束规范)
7. [表结构模板](#表结构模板)
8. [v4.0 改动](#v40-改动)
9. [检查清单](#检查清单)
10. [常见错误](#常见错误)

---

## 核心原则

### 基本规则
- 表名：小写+下划线，格式 `业务前缀_表名`
- 字段名：小写+下划线，避免 SQL 关键字
- 字符集：`utf8mb4`，排序规则 `utf8mb4_unicode_ci`
- 引擎：`InnoDB`
- 主键：`BIGINT AUTO_INCREMENT`
- 外键：不使用约束，应用层控制

---

## 命名规范

### 字段命名规则表

| 字段类型 | 命名规则 | 示例 |
|---------|---------|------|
| 主键 | `id` | `id BIGINT AUTO_INCREMENT` |
| 外键 | `{表名}_id` | `tenant_id`, `module_id` |
| 状态 | `{业务}_status` | `module_status`, `student_status` |
| 类型 | `{业务}_type` | `module_type`, `course_type` |
| 名称 | `{业务}_name` | `module_name`, `class_name` |
| 时间 | `{动作}_date` | `created_date`, `updated_date` |
| 流水号 | `serial_number` 或 `{业务}_no` | `serial_number`, `student_no` |

### 二值字段命名（根据词性）

**过去时 → 直接使用，不加后缀**
```sql
`deleted` TINYINT NOT NULL DEFAULT 0 COMMENT '是否删除: 0-未删除, 1-已删除'
`archived` TINYINT NOT NULL DEFAULT 0 COMMENT '是否归档: 0-未归档, 1-已归档'
`published` TINYINT NOT NULL DEFAULT 0 COMMENT '是否发布: 0-未发布, 1-已发布'
```

**形容词 → 直接使用，不加后缀**
```sql
`visible` TINYINT NOT NULL DEFAULT 1 COMMENT '是否可见: 0-不可见, 1-可见'
`required` TINYINT NOT NULL DEFAULT 0 COMMENT '是否必填: 0-非必填, 1-必填'
`searchable` TINYINT NOT NULL DEFAULT 1 COMMENT '是否可搜索: 0-不可搜索, 1-可搜索'
`sortable` TINYINT NOT NULL DEFAULT 1 COMMENT '是否可排序: 0-不可排序, 1-可排序'
`active` TINYINT NOT NULL DEFAULT 1 COMMENT '是否激活: 0-未激活, 1-已激活'
`enabled` TINYINT NOT NULL DEFAULT 1 COMMENT '是否启用: 0-禁用, 1-启用'
```

**名词 → 加 `_flag` 后缀**
```sql
`leaf_flag` TINYINT NOT NULL DEFAULT 1 COMMENT '是否叶子节点: 0-否, 1-是'
`root_flag` TINYINT NOT NULL DEFAULT 0 COMMENT '是否根节点: 0-否, 1-是'
`parent_flag` TINYINT NOT NULL DEFAULT 0 COMMENT '是否父节点: 0-否, 1-是'
```

---

## 数据类型

### 整数类型

| 用途 | 类型 | 说明 |
|------|------|------|
| 主键、外键 | `BIGINT` | 支持大数据量 |
| 数量、排序、层级 | `INT` | 范围较小的字段 |
| 布尔值 | `TINYINT` | 0/1 表示，不使用 TINYINT(1) |

### 字符串类型

| 用途 | 类型 | 长度 |
|------|------|------|
| 编码、状态 | `VARCHAR` | 20 |
| 用户ID | `VARCHAR` | 50 |
| 名称、标题 | `VARCHAR` | 100 |
| 描述、备注 | `TEXT` | - |

### 日期时间

| 用途 | 类型 | 说明 |
|------|------|------|
| 日期时间 | `DATETIME` | 精确到秒 |
| 仅日期 | `DATE` | 生日、考试日期 |

### 其他类型

| 用途 | 类型 | 说明 |
|------|------|------|
| 金额、分数 | `DECIMAL(M,D)` | M总位数，D小数位 |
| 配置、映射 | `JSON` | 原生 JSON 类型 |

---

## 审计字段

**每张表必须包含以下 7 个审计字段**（按顺序）：

```sql
`created_by` VARCHAR(50) COMMENT '创建人ID',
`created_date` DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP COMMENT '创建时间',
`created_name` VARCHAR(100) COMMENT '创建人姓名',
`updated_by` VARCHAR(50) COMMENT '更新人ID',
`updated_date` DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP COMMENT '更新时间',
`updated_name` VARCHAR(100) COMMENT '更新人姓名',
`deleted` TINYINT NOT NULL DEFAULT 0 COMMENT '是否删除: 0-未删除, 1-已删除'
```

| 字段 | 类型 | 必填 | 说明 |
|------|------|------|------|
| `created_by` | VARCHAR(50) | 否 | 创建人用户ID |
| `created_date` | DATETIME | 是 | 创建时间，自动填充 |
| `created_name` | VARCHAR(100) | 否 | 创建人姓名 |
| `updated_by` | VARCHAR(50) | 否 | 更新人用户ID |
| `updated_date` | DATETIME | 是 | 更新时间，自动更新 |
| `updated_name` | VARCHAR(100) | 否 | 更新人姓名 |
| `deleted` | TINYINT | 是 | 逻辑删除标记 |

---

## 索引规范

### 命名规则

| 索引类型 | 格式 | 示例 |
|---------|------|------|
| 主键 | `PRIMARY KEY` | `PRIMARY KEY (id)` |
| 唯一索引 | `uk_{字段组合}` | `uk_tenant_module_code` |
| 普通索引 | `idx_{字段名}` | `idx_module_status` |

### 必须建立索引的字段

- 外键字段：`tenant_id`, `module_id`, `parent_id`
- 状态字段：`module_status`, `student_status`
- 查询条件：`deleted`, `leaf_flag`
- 时间字段：`created_date`
- 组合查询：`(tenant_id, module_status)`

**示例**:
```sql
PRIMARY KEY (`id`),
UNIQUE KEY `uk_tenant_module_code` (`tenant_id`, `module_code`),
INDEX `idx_tenant_id` (`tenant_id`),
INDEX `idx_module_status` (`module_status`),
INDEX `idx_deleted` (`deleted`),
INDEX `idx_created_date` (`created_date`)
```

---

## 约束规范

### 外键约束
**不使用外键约束**，原因：
- 性能开销
- 灵活性（分库分表）
- 微服务友好
- 应用层控制

### 检查约束

```sql
CONSTRAINT `chk_permission` CHECK (`permission` BETWEEN 0 AND 7)
CONSTRAINT `chk_gender` CHECK (`gender` IN (0, 1, 2))
```

---

## 表结构模板

```sql
CREATE TABLE `表名` (
  -- 主键
  `id` BIGINT NOT NULL AUTO_INCREMENT COMMENT '主键ID',
  
  -- 租户字段（多租户表必需）
  `tenant_id` BIGINT NOT NULL DEFAULT 0 COMMENT '租户ID',
  
  -- 业务字段
  `字段名` 类型 约束 COMMENT '字段说明',
  
  -- 审计字段
  `created_by` VARCHAR(50) COMMENT '创建人ID',
  `created_date` DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP COMMENT '创建时间',
  `created_name` VARCHAR(100) COMMENT '创建人姓名',
  `updated_by` VARCHAR(50) COMMENT '更新人ID',
  `updated_date` DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP COMMENT '更新时间',
  `updated_name` VARCHAR(100) COMMENT '更新人姓名',
  `deleted` TINYINT NOT NULL DEFAULT 0 COMMENT '是否删除: 0-未删除, 1-已删除',
  
  -- 主键
  PRIMARY KEY (`id`),
  
  -- 唯一索引
  UNIQUE KEY `uk_字段组合` (`字段1`, `字段2`),
  
  -- 普通索引
  INDEX `idx_字段名` (`字段名`),
  INDEX `idx_deleted` (`deleted`)
    
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci COMMENT='表说明';
```

---

## v4.0 改动

### 改动清单

| 改动项 | 改动前 | 改动后 | 说明 |
|--------|--------|--------|------|
| 二值字段 | `is_deleted` | `deleted` | 过去时不加后缀 |
| 二值字段 | `is_visible` | `visible` | 形容词不加后缀 |
| 二值字段 | `is_leaf` | `leaf_flag` | 名词加 _flag |
| 状态字段 | `status` | `module_status` | 加业务前缀 |
| 类型字段 | `type` | `module_type` | 加业务前缀 |
| 名称字段 | `name` | `module_name` | 加业务前缀 |
| 时间字段 | `created_at` | `created_date` | 使用 _date |
| 时间字段 | `updated_at` | `updated_date` | 使用 _date |
| 审计字段 | 无 | 7个审计字段 | 每张表必需 |

### 迁移脚本示例

```sql
-- sys_module 表迁移
ALTER TABLE sys_module 
  CHANGE COLUMN `status` `module_status` VARCHAR(20) NOT NULL DEFAULT 'active',
  CHANGE COLUMN `is_leaf` `leaf_flag` TINYINT NOT NULL DEFAULT 1,
  CHANGE COLUMN `is_deleted` `deleted` TINYINT NOT NULL DEFAULT 0,
  CHANGE COLUMN `created_at` `created_date` DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP,
  CHANGE COLUMN `updated_at` `updated_date` DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
  ADD COLUMN `created_name` VARCHAR(100) AFTER `created_by`,
  ADD COLUMN `updated_name` VARCHAR(100) AFTER `updated_by`;

-- 更新索引
ALTER TABLE sys_module 
  DROP INDEX `idx_status`,
  DROP INDEX `idx_is_leaf`,
  DROP INDEX `idx_is_deleted`,
  ADD INDEX `idx_module_status` (`module_status`),
  ADD INDEX `idx_leaf_flag` (`leaf_flag`),
  ADD INDEX `idx_deleted` (`deleted`);
```

---

## 检查清单

创建或修改表时，请检查以下项目：

- [ ] 表名使用小写+下划线
- [ ] 主键使用 `id BIGINT AUTO_INCREMENT`
- [ ] 外键字段使用 `_id` 后缀
- [ ] 状态字段使用业务前缀
- [ ] 布尔字段根据词性命名
- [ ] 时间字段使用 `_date` 后缀
- [ ] 包含完整的 7 个审计字段
- [ ] 多租户表包含 `tenant_id` 字段
- [ ] 所有字段都有 COMMENT
- [ ] 表有 COMMENT
- [ ] 为常用查询字段建立索引
- [ ] 唯一约束使用 UNIQUE KEY
- [ ] 不使用外键约束
- [ ] 使用 InnoDB 引擎
- [ ] 使用 utf8mb4 字符集

---

## 常见错误

| ❌ 错误 | ✅ 正确 | 原因 |
|--------|--------|------|
| `is_deleted` | `deleted` | 过去时不需要 `is_` 和 `_flag` |
| `is_visible` | `visible` | 形容词不需要 `is_` 和 `_flag` |
| `is_leaf` | `leaf_flag` | 名词需要 `_flag`，不需要 `is_` |
| `visible_flag` | `visible` | 形容词不需要 `_flag` |
| `deleted_flag` | `deleted` | 过去时不需要 `_flag` |
| `leaf` | `leaf_flag` | 名词需要 `_flag` |
| `status` | `module_status` | 状态字段需要业务前缀 |
| `type` | `module_type` | 类型字段需要业务前缀 |
| `name` | `module_name` | 名称字段需要业务前缀 |
| `TINYINT(1)` | `TINYINT` | 不使用长度限制 |
| `created_at` | `created_date` | 使用 _date 后缀 |
| `updated_at` | `updated_date` | 使用 _date 后缀 |

---

## 完整表示例

### sys_module 表

```sql
CREATE TABLE `sys_module` (
  `id` BIGINT NOT NULL AUTO_INCREMENT COMMENT '主键ID',
  `tenant_id` BIGINT NOT NULL DEFAULT 0 COMMENT '租户ID',
  `module_code` VARCHAR(50) NOT NULL COMMENT '模块编码',
  `module_name` VARCHAR(100) NOT NULL COMMENT '模块名称',
  `module_type` VARCHAR(20) NOT NULL DEFAULT 'query' COMMENT '模块类型',
  `module_status` VARCHAR(20) NOT NULL DEFAULT 'active' COMMENT '模块状态',
  `leaf_flag` TINYINT NOT NULL DEFAULT 1 COMMENT '是否叶子节点: 0-否, 1-是',
  `sort_order` INT NOT NULL DEFAULT 0 COMMENT '排序顺序',
  `parent_id` BIGINT COMMENT '父模块ID',
  `created_by` VARCHAR(50) COMMENT '创建人ID',
  `created_date` DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP COMMENT '创建时间',
  `created_name` VARCHAR(100) COMMENT '创建人姓名',
  `updated_by` VARCHAR(50) COMMENT '更新人ID',
  `updated_date` DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP COMMENT '更新时间',
  `updated_name` VARCHAR(100) COMMENT '更新人姓名',
  `deleted` TINYINT NOT NULL DEFAULT 0 COMMENT '是否删除: 0-未删除, 1-已删除',
  PRIMARY KEY (`id`),
  UNIQUE KEY `uk_tenant_module_code` (`tenant_id`, `module_code`),
  INDEX `idx_tenant_id` (`tenant_id`),
  INDEX `idx_module_status` (`module_status`),
  INDEX `idx_leaf_flag` (`leaf_flag`),
  INDEX `idx_deleted` (`deleted`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci COMMENT='模块配置表';
```

