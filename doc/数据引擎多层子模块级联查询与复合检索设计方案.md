# 数据引擎多层子模块级联查询与复合检索设计方案 (100% 严密自洽版)

## 一、方案背景与核心命题

在低代码平台的数据引擎中，复杂的业务对象往往由一个核心主实体及其衍生的伴生实体、多层级从属实体共同构成。例如在“学生综合档案（101）”模块中，存在如下关系链条：
- 主表：`student`（学生基础信息）
- 1:1 伴生表：`student_profile`（扩展档案）
- N:1 维表：`clazz`（所属班级）
- 1:N 从表：`student_course`（选课明细）
- 1:N 孙表：`student_course_score_item`（课程考核构成明细）
- 1:N 曾孙表：`score_item_deduction`（分项扣分记录）

### 核心业务诉求
1. **全字段任意复合检索**：列表表头或检索面板中配置的任意层级字段（无论是主表、伴生表、还是 1:N:N:...:N 深层孙表字段），均可直接参与列表过滤；
2. **多层级数据一次性完整输出**：列表查询返回的数据行必须自动包含完整嵌套的子模块明细树；
3. **主表分页严格自洽**：分页 `LIMIT/OFFSET` 必须严格作用于主记录，绝不因 1:N 展开产生笛卡尔积膨胀或分页错乱；
4. **100% 严密自洽，杜绝猜测**：消除任何基于运行时盲搜（BFS 图搜索）产生的歧义（如菱形依赖多路径、中间表二义性）。

---

## 二、数学证明与第一性原理

### 1. 为什么“图搜索（Graph Search）”无法做到 100% 严密？
在一般的实体关系图（Entity-Relationship Graph）中，两张物理表之间可能存在多条合法的业务路径（例如 `project` 到 `sys_user` 既可以通过 `manager_id` 关联，也可以通过 `project_member` 中间表关联）。若运行时仅依据物理表名进行 BFS/DFS 搜索，必然存在信息论上的二义性死结（Ambiguity），导致算法有概率猜错业务意图。

### 2. 核心定理（模块树与路径唯一性）
> **定理（模块连接树定理）**：
> 若模块与子模块的组织结构构成一棵以根模块（Root Module）为根节点的有向有根树 $T = (V, E)$，且树中每个非根节点 $v \in V \setminus \{root\}$ 到其父节点 $parent(v)$ 均绑定唯一的表连接关系 $R(v) \in \text{sys\_table\_relation}$，则：
> 对于模块树中任意节点 $u$ 所属的任意字段 $f$，从根主表到字段所属物理表的 **JOIN 路径链条 $P(root, u)$ 在数学上有且仅有一条**。

**证明**：
根据图论基本性质，树是连通且无环的无向图。在有根树中，从根节点 $root$ 到任意节点 $u$ 的简单路径具有唯一性。因为每条树边 $(p, c)$ 都严格映射到一个确定的一对一或一对多物理表关联规则，故物理表级联连接链：
$$P = [ R(v_1), R(v_2), \dots, R(u) ] \quad (\text{其中 } root = v_0 \to v_1 \to \dots \to v_k = u)$$
也是唯一确定的，不存在任何分支选择或歧义。$\blacksquare$

---

## 三、元数据模型设计（Metadata Model）

要实现上述数学保证，元数据体系必须严格遵循 **“模块连接树（Module Join Tree）”** 规范：

```
                           [101] 学生综合档案 (Root)
                           Primary Table: student
                           伴生表: clazz (N:1), student_profile (1:1)
                                      │
                 ┌────────────────────┴────────────────────┐
                 ▼ (rel_1)                                 ▼ (rel_2)
          [102] 选课明细 (1:N)                       [103] 荣誉奖项 (1:N)
          Table: student_course                     Table: student_award
          Relation: student.id                      Relation: student.id
                  = sc.student_id                           = sa.student_id
                 │
                 ▼ (rel_3)
          [104] 考核构成明细 (1:N)
          Table: student_course_score_item
          Relation: sc.id = scsi.course_id
                 │
                 ▼ (rel_4)
          [105] 扣分细则 (1:N)
          Table: score_item_deduction
          Relation: scsi.id = sid.item_id
```

### 1. 模块表结构定义 (`sys_module`)
- `id`: 模块主键 ID
- `parent_id`: 父模块 ID（根模块为 0）
- `primary_table`: 当前模块的基准物理表名
- `relation_id`: 当前模块与其父模块 `parent_id` 之间的连接关系 ID（根模块为 NULL）

### 2. 字段元数据定义 (`sys_module_field`)
- `id`: 字段主键 ID
- `module_id`: 归属的模块 ID（**关键**：通过 `module_id` 定位其在模块树中的绝对坐标，从而唯一锁定 JOIN 路径）
- `table_name`: 物理表名
- `column_name`: 物理列名

### 3. 表关联关系定义 (`sys_table_relation`)
- `id`: 关系主键 ID
- `main_table`: 主键提供方物理表
- `main_field`: 主键字段名（如 `id`）
- `join_table`: 外键持有方物理表
- `join_field`: 外键字段名（如 `student_id`）
- `relation_type`: `1:1` 或 `1:N`

---

## 四、菱形依赖与自关联的消歧建模

### 1. 菱形依赖（Diamond Dependency）建模
如果业务中同一张物理表 `D` 可以通过两条不同的业务线连接到根实体 `A`：
- 路径 1：$A \xrightarrow{rel\_1} B \xrightarrow{rel\_3} D$
- 路径 2：$A \xrightarrow{rel\_2} C \xrightarrow{rel\_4} D$

**严密建模规则**：
在模块树中，表 `D` 必须被注册为两个不同的子模块节点（例如 `module_D1` 和 `module_D2`）：
```
        Module A (Root)
        ├── Module B (Parent=A, Relation=rel_1)
        │   └── Module D_via_B (Parent=B, Relation=rel_3, Table=D)
        │       └── 字段 D.name (归属 D_via_B) ──> 路径唯一确定: A -> B -> D
        │
        └── Module C (Parent=A, Relation=rel_2)
            └── Module D_via_C (Parent=C, Relation=rel_4, Table=D)
                └── 字段 D.name (归属 D_via_C) ──> 路径唯一确定: A -> C -> D
```
每个字段通过其绑定的 `module_id` 拥有唯一的上溯路径，**彻底消除菱形二义性**。

---

## 五、不同模块复用同表同字段的消歧与隔离机制

### 1. 业务场景与碰撞痛点
在复杂业务中，`moduleHeaders` 常包含多个子模块字段，且极可能出现**不同模块复用同一个物理表的同一个字段**：
- **场景**：主模块（101）配置了 `sys_user.user_name`（班主任姓名），选课子模块（102）配置了 `sys_user.user_name`（任课老师姓名），考核孙模块（104）配置了 `sys_user.user_name`（阅卷老师姓名）。
- **痛点**：若仅靠 `tableName.columnName` 标识，会导致 SQL 别名冲突、字段互相覆盖、检索条件二义性。

### 2. 核心解决方案：三元组唯一坐标体系
每个字段在系统中的绝对标识严格定义为三元组：
$$\text{Field Coordinate} = (\text{moduleId}, \text{tableName}, \text{columnName})$$

1. **SQL 别名隔离**：
   - 依赖的物理表在 SQL 中使用带模块 ID 前缀的独立别名（如 `u_101`、`u_102`、`u_104`）；
   - 投影列别名采用 `m{moduleId}__{tableName}__{columnName}` 唯一化命名；
2. **条件构造精准下推**：
   - 属于主模块（101）的 `sys_user` 过滤条件，直接作为外层主查询的 `u_101.user_name LIKE ...`；
   - 属于选课子模块（102）的 `sys_user` 过滤条件，精准下推到 `EXISTS (SELECT 1 FROM student_course sc JOIN sys_user u_102 ...)` 内部；
3. **数据装配归属明确**：
   - 内存装配时，按 `moduleId` 归集字段数据，外层主对象拿到班主任，子对象拿到任课老师，孙对象拿到阅卷老师，层级完全隔离，零碰撞。

---

## 六、动态 SQL 生成算法（确定性复合检索）

当列表查询发起，接收到前端传入的三元组检索条件 `List<(moduleId, tableName, columnName, value)>` 时：

### 1. 极速构建流水线（3 步流水线）
1. **按 `moduleId` 归组 (Scope Grouping)**：
   将全部过滤条件按 `moduleId` 进行分组，划分为主模块作用域条件和各子模块分支条件；
2. **根模块直连下推**：
   主模块（101）自身的字段与伴生表条件，直接作用于外层主 SQL 的 `WHERE`，伴生表使用 `m{rootId}_{tableName}` 隔离别名；
3. **子模块支路自动融合 (Branch Merging)**：
   对于处于同一层级链路的子模块条件（如 102 选课与 104 考核分项），算法自动合并为一条多表连接的 `EXISTS (SELECT 1 FROM sc JOIN scsi ...)` 子查询，避免多次重复子查询，达到最优索引性能。

### 2. 生成 SQL 完整形态样例
```sql
SELECT 
    `student`.`id` AS `student__id`,
    `student`.`name` AS `name`,
    `m101_sys_user`.`user_name` AS `m101__sys_user__user_name`,
    `m101_clazz`.`clazz_name` AS `m101__clazz__clazz_name`
FROM `student`
LEFT JOIN `sys_user` `m101_sys_user` 
    ON `m101_sys_user`.`id` = `student`.`headmaster_id` AND `m101_sys_user`.`deleted` = 0
LEFT JOIN `clazz` `m101_clazz` 
    ON `m101_clazz`.`id` = `student`.`clazz_id` AND `m101_clazz`.`deleted` = 0
WHERE `student`.`deleted` = 0
  -- 根模块条件
  AND `student`.`name` LIKE '%张三%'
  AND `m101_sys_user`.`user_name` LIKE '%王班主任%'
  
  -- 102 选课与 104 考核明细融合后的单一 EXISTS 子查询
  AND EXISTS (
      SELECT 1 
      FROM `student_course` `m102_student_course`
      LEFT JOIN `sys_user` `m102_sys_user` 
        ON `m102_sys_user`.`id` = `m102_student_course`.`teacher_id` AND `m102_sys_user`.`deleted` = 0
      JOIN `student_course_score_item` `m104_score_item` 
        ON `m104_score_item`.`course_id` = `m102_student_course`.`id` AND `m104_score_item`.`deleted` = 0
      WHERE `m102_student_course`.`student_id` = `student`.`id`
        AND `m102_student_course`.`deleted` = 0
        AND `m102_student_course`.`course_name` LIKE '%高数%'
        AND `m102_sys_user`.`user_name` LIKE '%李任课老师%'
        AND `m104_score_item`.`score` >= 90
  )
ORDER BY `student`.`id` DESC
LIMIT 10 OFFSET 0;
```

---

### 2. 结构化 SQL 组装规则
遍历所有检索条件，根据解析得到的 `steps` 路径进行分类：

1. **若 `steps.isEmpty()`（字段属于根模块主表）**：
   - 直接作为主查询的 `WHERE` 条件：`student.name LIKE '%张%'`。
2. **若 `steps` 中所有关系均为 `1:1` 或 `N:1`（平铺伴生表）**：
   - 主 SQL 自动 `LEFT JOIN clazz ON ...`，并在主 `WHERE` 中追加条件：`clazz.clazz_name LIKE '%一班%'`。
3. **若 `steps` 中包含任意 `1:N` 关系（多层级子模块）**：
   - 提取从根模块出发的第一条 1:N 边到目标孙表的所有连续边；
   - 自动合并相同子路径的过滤条件，组装为单一的 `WHERE EXISTS` 子查询：

```sql
EXISTS (
    SELECT 1 
    FROM `student_course` `sc`
    JOIN `student_course_score_item` `scsi` 
      ON `scsi`.`course_id` = `sc`.`id` AND `scsi`.`deleted` = 0
    JOIN `score_item_deduction` `sid`
      ON `sid`.`item_id` = `scsi`.`id` AND `sid`.`deleted` = 0
    WHERE `sc`.`student_id` = `student`.`id`
      AND `sc`.`deleted` = 0
      AND `scsi`.`item_name` = '期末考试'
      AND `sid`.`deduction_points` > 5
)
```

---

## 六、数据组装算法（Top-Down 批量并发提取 + Bottom-Up 递归归集）

### 1. 基于表头（Headers）与检索条件（Filters）的按需精准裁剪（On-Demand Pruning）
为了达到极致的查询性能，避免加载未勾选的冗余子表：
1. **提取激活表集合 (`activeTables`)**：从当前请求的 `moduleHeaders`（已勾选表头）和 `filters`（已输入检索条件）中提取所有涉及的物理表名集合；
2. **提取树动态裁剪 (Prune)**：自顶向下遍历模块树，若某个 1:N 子分支节点及其所有后代物理表均不在 `activeTables` 中，**直接整枝裁剪，不发起任何数据库查询**；
3. **伴生表精准 JOIN**：主 SQL 仅 `LEFT JOIN` 处于 `activeTables` 中的 1:1 和 N:1 伴生表，绝不多余关联。

### 2. 极速两阶段执行流程
```
1. [阶段一：主记录分页]
   执行主表 LEFT JOIN SQL (仅连激活的伴生表) + WHERE (含 EXISTS) + LIMIT 10 OFFSET 0
   获取当前页命中主键：primaryIds = [2001, 2002, 2005]

2. [阶段二：子模块按裁剪树分层批量拉取]
   - Level 1: (若 student_course 被激活)
     查 student_course WHERE student_id IN (2001, 2002, 2005) AND deleted = 0
     得到 courseIds = [501, 502, 503, 504]
   
   - Level 2: (若 student_course_score_item 被激活)
     查 student_course_score_item WHERE course_id IN (501, 502, 503, 504) AND deleted = 0
     得到 itemIds = [1001, 1002, 1003]

   - Level 3: (若 score_item_deduction 被激活)
     查 score_item_deduction WHERE item_id IN (1001, 1002, 1003) AND deleted = 0

3. [阶段三：内存自底向上归集 (Bottom-Up Assembly)]
   - 将 deduction 列表按 item_id 挂到 score_item 上；
   - 将 score_item 列表按 course_id 挂到 student_course 上；
   - 将 student_course 列表按 student_id 挂到 student 主记录上。
```

### 性能与正确性保证：
- **按需加载，性能极致**：若表头未勾选子模块，查询次数降为 **1 次**；若勾选了子模块，SQL 次数严格等于 **被激活的 1:N 节点数 + 1**，绝无 $N+1$；
- **分页 100% 精确**：主表 `COUNT(*)` 和分页 SQL 始终独立计算，彻底隔绝笛卡尔积膨胀。

---

## 七、权限体系与数据脱敏对接

在全链路统一采用 `tableName`、`columnName`、`view`、`apply`、`edit` 的规范下：
1. **数据脱敏**：在执行完阶段三的自底向上归集后，根据当前角色的 `view` 权限矩阵，遍历整棵结果树，对 `view == 0` 的字段执行置空或掩码（Masking）处理；
2. **写权限校验**：保存时严格按各层实体的物理表名与列名，校验 `apply`（新增）或 `edit`（修改）权限。

---

## 八、落地步骤与改造清单

| 模块/组件 | 涉及文件 | 改造任务 |
| :--- | :--- | :--- |
| **条件构造器** | `JooqConditionBuilder.java` | 引入 `JoinPathResolver`，基于模块树回溯生成确定性 `EXISTS ... JOIN` 条件并自动合并相同路径。 |
| **查询执行引擎** | `DynamicQueryService.java` | 实现基于模块树的通用 Top-Down 递归批量提取与 Bottom-Up 内存结构装配，列表模式默认开启子模块加载。 |
| **持久化服务** | `DynamicPersistenceService.java` | 对齐模块连接树的拓扑排序，按从根到叶的顺序执行原子级联持久化。 |
| **测试套件** | `DataEngineQueryTest.java` | 覆盖单表、1:1/N:1 伴生、1:N、1:N:N 任意深度孙表复合检索与全量结构化装配用例。 |


```sql
SELECT count(*)
FROM `school`.`student`
LEFT JOIN `school`.`clazz` `101_clazz` 
  ON `101_clazz`.`id` = `student`.`clazz_id` AND `101_clazz`.`deleted` = 0
LEFT JOIN `school`.`student_profile` `101_student_profile` 
  ON `101_student_profile`.`student_id` = `student`.`id` AND `101_student_profile`.`deleted` = 0
WHERE `student`.`deleted` = 0
  -- 1. 严格限定在 101 根模块自身的检索条件
  AND `student`.`name` LIKE '%张三%'
  AND `101_clazz`.`clazz_name` LIKE '%计科%'

  -- 2. 支路一（103 选课支路：融合从表 student_course 与孙表 student_course_score_item）
  AND EXISTS (
      SELECT 1 
      FROM `school`.`student_course` `103_student_course`
      JOIN `school`.`student_course_score_item` `103_student_course_score_item` 
        ON `103_student_course_score_item`.`student_course_id` = `103_student_course`.`id` 
       AND `103_student_course_score_item`.`deleted` = 0
      WHERE `103_student_course`.`student_id` = `student`.`id`
        AND `103_student_course`.`deleted` = 0
        -- 严格限定在 103 选课模块范围内的条件：
        AND `103_student_course`.`course_name` LIKE '%高等数学%'
        AND `103_student_course_score_item`.`score` >= 90
  )

  -- 3. 支路二（104 荣誉与 106 佐证明细支路：融合 student_award 与 student_award_detail）
  AND EXISTS (
      SELECT 1 
      FROM `school`.`student_award` `104_student_award`
      JOIN `school`.`student_award_detail` `106_student_award_detail` 
        ON `106_student_award_detail`.`award_id` = `104_student_award`.`id` 
       AND `106_student_award_detail`.`deleted` = 0
      WHERE `104_student_award`.`student_id` = `student`.`id`
        AND `104_student_award`.`deleted` = 0
        -- 严格限定在 104 荣誉模块的条件：
        AND `104_student_award`.`award_name` LIKE '%国家奖学金%'
        -- 严格限定在 106 佐证孙模块的条件：
        AND `106_student_award_detail`.`issuing_authority` LIKE '%教育部%'
  );
```

```sql
SELECT 
    `student`.`id` AS `id`,
    `student`.`student_no` AS `101__student__student_no`,
    `student`.`name` AS `101__student__name`,
    `101_clazz`.`clazz_name` AS `101__clazz__clazz_name`,
    `101_clazz`.`grade` AS `101__clazz__grade`,
    `101_student_profile`.`emergency_phone` AS `101__student_profile__emergency_phone`,
    `101_clazz`.`id` AS `clazz__id`,
    `101_student_profile`.`id` AS `student_profile__id`
FROM `school`.`student`
LEFT JOIN `school`.`clazz` `101_clazz` 
  ON `101_clazz`.`id` = `student`.`clazz_id` AND `101_clazz`.`deleted` = 0
LEFT JOIN `school`.`student_profile` `101_student_profile` 
  ON `101_student_profile`.`student_id` = `student`.`id` AND `101_student_profile`.`deleted` = 0
WHERE `student`.`deleted` = 0
  -- 根模块条件
  AND `student`.`name` LIKE '%张三%'
  AND `101_clazz`.`clazz_name` LIKE '%计科%'

  -- 103 选课与分项明细融合 EXISTS
  AND EXISTS (
      SELECT 1 
      FROM `school`.`student_course` `103_student_course`
      JOIN `school`.`student_course_score_item` `103_student_course_score_item` 
        ON `103_student_course_score_item`.`student_course_id` = `103_student_course`.`id` 
       AND `103_student_course_score_item`.`deleted` = 0
      WHERE `103_student_course`.`student_id` = `student`.`id`
        AND `103_student_course`.`deleted` = 0
        AND `103_student_course`.`course_name` LIKE '%高等数学%'
        AND `103_student_course_score_item`.`score` >= 90
  )

  -- 104 荣誉与 106 佐证明细融合 EXISTS
  AND EXISTS (
      SELECT 1 
      FROM `school`.`student_award` `104_student_award`
      JOIN `school`.`student_award_detail` `106_student_award_detail` 
        ON `106_student_award_detail`.`award_id` = `104_student_award`.`id` 
       AND `106_student_award_detail`.`deleted` = 0
      WHERE `104_student_award`.`student_id` = `student`.`id`
        AND `104_student_award`.`deleted` = 0
        AND `104_student_award`.`award_name` LIKE '%国家奖学金%'
        AND `106_student_award_detail`.`issuing_authority` LIKE '%教育部%'
  )
ORDER BY `student`.`id` DESC
LIMIT 20 OFFSET 0;

```